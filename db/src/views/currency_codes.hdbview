VIEW "ISO.CURRNECY_CODES" ( 
     "CODE",
	 "CURRENCY" ) AS SELECT
	 DISTINCT "CURRENCIES_$0"."CODE" ,
	 "CURRENCIES_$0"."CURRENCY" 
FROM "ISO.CURRENCIES" AS "CURRENCIES_$0" 
ORDER BY "CODE" WITH READ ONLY